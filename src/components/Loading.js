import React from "react";

function Loading() {
  return (
    <div
      style={{
        fontSize: "25px",
        fontWeight: "bold",
        textAlign: "center",
        color: "green",
      }}
    >
      Loading..
    </div>
  );
}

export default Loading;
